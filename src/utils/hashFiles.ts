import { input } from './inputs';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function hashFiles(...globs: string[]): Promise<string> {
  // TODO: implement correctly
  return await input('ref');
}
